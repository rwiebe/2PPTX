version: '3'
services:
  app:
    image: 2pptx:main
    build: .
    ports:
      - "$PORT:5000"
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    restart: always
    networks:
      nginx_proxy_net:
networks:
  nginx_proxy_net:
